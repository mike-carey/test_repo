---

name: 'Test'

on: [workflow_dispatch]

defaults:
  run:
    shell: bash

jobs:
  test:
    name: 'Test'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2

      - uses: ./.github/actions/test

      - name: 'Status'
        if: always()
        run: echo ${{ job.status }}

  amicrazy:
    name: 'Am I crazy?'
    runs-on: ubuntu-latest
    steps:
      - name: 'Fail'
        run: exit 1

      - name: 'Status'
        if: always()
        run: echo ${{ job.status }}

  test-2:
    name: 'Test 2'
    uses: ./.github/reusable/test.yml
